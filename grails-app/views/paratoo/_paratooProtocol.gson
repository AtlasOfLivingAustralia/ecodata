import au.org.ala.ecodata.ActivityForm

model {
    ActivityForm protocol
}

json {
    identifier protocol.externalId
    name protocol.name
    version protocol.formVersion as Integer
    module protocol.category
}